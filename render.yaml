services:
  - type: web
    name: wdd330-team05
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node main.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
